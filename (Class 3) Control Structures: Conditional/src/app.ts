let a; // Let declares a new variable
let b: string = "s"; // string indicates the text type
let c: number; // Number indicates numeric types, both real and whole;
function d() {} // The function keyword declares a new function, equivalent to let for variables
if (2 > 3) {} // The if declares a conditional expression
else {} // The else keyword defines what happens when if evaluates false
for (;false;) {} // For decleares a sequential cycle
while(false) {} // While declares a conditional cycle
function f() {return ""} // Return is a special keyword for functions
switch(""){} // Switch is used for multivalue selection
switch(b){case "s": break;} // Case indicates one of the values handled by switch, break stops execution
try {} catch(e){} finally {} // Try and catch are used for exception handling
const g = 3; // Const is used to declare constant variables
class X {} // Declares a new class
export default class Y {} // export and default are used to handle modules
async function Z() {await ""} // async/await are used to handle asynchronous operations